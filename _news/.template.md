---
layout: post
date: 2026-01-01 12:00:00-0500
inline: true
related_posts: false
---

TODO(jane): copy this file to _news/<something>.md, set a real date, and write one
sentence. Jekyll ignores dotfiles, so this template itself never publishes.

Inline entries (inline: true) render as a single dated line on the home page and on
/news/. For a longer item, set inline: false and add a `title:` - it becomes its own
page and the home page links to the title.

Likely first entries: the ICMLA 2025 paper, the EpiCast competition result, and the
defense date.
